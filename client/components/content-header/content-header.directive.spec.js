'use strict';

describe('Directive: contentHeader', function () {

  //// load the directive's module and view
  //beforeEach(module('digitalVillageApp'));
  //beforeEach(module('components/content-header/content-header.html'));
  //
  //var element, scope;
  //
  //beforeEach(inject(function ($rootScope) {
  //  scope = $rootScope.$new();
  //}));
  //
  //it('should make hidden element visible', inject(function ($compile) {
  //  element = angular.element('<content-header></content-header>');
  //  element = $compile(element)(scope);
  //  scope.$apply();
  //  expect(element.text()).toBe('this is the contentHeader directive');
  //}));
});
